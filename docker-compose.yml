services:
  web:
    container_name: vut-tasker-web
    image: nginx:latest
    volumes:
      - ./web/dist:/usr/share/nginx/html
    environment:
      - NGINX_PORT=80
    ports:
      - 80:80
    restart: unless-stopped
  api:
    depends_on:
      - mongodb
    container_name: vut-tasker-api
    build:
      dockerfile: ./server/Dockerfile
      context: ./
    ports:
      - 3000:3000
    volumes:
      - ./server/src:/usr/src/app/src
      - ./shared:/usr/src/shared
      - ./.env:/usr/src/.env
    restart: unless-stopped
    networks:
      servers-network:
        ipv4_address: 192.168.0.202
      # vut-tasker-prod-network:
  mongodb:
    container_name: mongodb
    image: mongo
    restart: unless-stopped
    ports:
      - ${DATABASE_PORT}:27017
    volumes:
      - ./mongodb:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${DATABASE_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${DATABASE_PASSWORD}
    networks:
      servers-network:
        ipv4_address: 192.168.0.203
      # vut-tasker-prod-network:
  cloudflare:
    restart: unless-stopped
    container_name: vut-tasker-prod-cloudflare
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run --token eyJhIjoiNmMxMWNjMWRmN2Q5OTI4YzA5NzI0ZGYwMjE0NzUwZWMiLCJ0IjoiYThkNWE4ZmUtMTc4NS00MTNiLWEyNDQtM2M5NzE1MDQ3ZjJiIiwicyI6Ik1HVXdZVGcwWkRjdE5qUm1ZeTAwWm1SbExUaGlPVE10TmpjNU9UbGhaR1psTmpBeCJ9
    networks:
      servers-network:
        ipv4_address: 192.168.0.201
networks:
  servers-network:
    external: true
    # vut-tasker-prod-network:
    #   name: vut-tasker-prod-network
    # driver: macvlan
    # driver_opts:
    # parent: eth0
    # ipam:
    # config:
    # - subnet: "192.168.0.0/24"
    # gateway: "192.168.0.1"
