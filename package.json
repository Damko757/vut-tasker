{
  "name": "vut-tasker-server",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "web": "cd ./web && bun run dev && cd ..",
    "web build": "cd ./web && bun run build && cd ..",
    "server": "cd ./server && bun run hot && cd .."
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}